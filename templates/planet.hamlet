<div .code>
    <div #code>

    <script>
        window.onload = function() {
                            var node = document.getElementById('code');
                            var app = Elm.Main.embed(node, #{foo});
                        }
